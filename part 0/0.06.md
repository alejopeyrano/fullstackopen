sequenceDiagram
participant browser
participant server

    Note right of browser: User writes a note and clicks Save (SPA)

    browser->>server: POST https://studies.cs.helsinki.fi/exampleapp/new_note_spa\n(JSON: {content: "...", date: "..."})
    activate server
    server-->>browser: 201 Created (or 200 OK)
    deactivate server

    Note right of browser: Browser updates the notes list in memory and re-renders the UI (no redirect)